# i3 config


##################
#                #
#     xev        #
#     cprop      #
#                #
##################


### Startup

# start default applications
exec --no-startup-id i3-msg "workspace --no-auto-back-and-forth 1; append_layout .i3/ws1; workspace 2; append_layout .i3/ws2; workspace 3; append_layout .i3/ws3; workspace 1"
#exec --no-startup-id spotify
exec --no-startup-id thunderbird
exec --no-startup-id firefox
exec --no-startup-id i3-sensible-terminal


## General Behaviour

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
#font pango:DejaVu Sans Mono, Terminus Bold Semi-Condensed 8
font pango:DejaVu Sans Mono, FontAwesome 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+q focus child

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
set $mode_system Options: (l) lock, (e) logout, (s) suspend, (r) reboot, (p) poweroff
mode "$mode_system" {
    bindsym l mode "default", exec --no-startup-id sleep 0.17 && $Locker
    bindsym e mode "default", exec --no-startup-id i3-msg exit
    bindsym s mode "default", exec --no-startup-id sleep 0.17 && $Locker && systemctl suspend
    bindsym r mode "default", exec --no-startup-id systemctl reboot
    bindsym p mode "default", exec --no-startup-id systemctl poweroff -i
    bindsym XF86PowerOff mode "default", exec --no-startup-id systemctl poweroff -i

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Shift+e mode "$mode_system"
bindsym XF86PowerOff mode "$mode_system"

# lock screen
#bindsym $mod+l exec i3lock-fancy-dualmonitor
set $locker i3lock-fancy-rapid 10 3 -e -k --pass-media-keys --pass-screen-keys --pass-power-keys --insidecolor=0000001c --ringcolor=0000003e --linecolor=00000000 --keyhlcolor=ffffff80 --ringvercolor=ffffff00 --separatorcolor=22222260 --insidevercolor=ffffff1c --ringwrongcolor=ffffff55 --insidewrongcolor=ffffff1c --verifcolor=ffffff00 --wrongcolor=ff000000 --timecolor=ffffff00 --datecolor=ffffff00 --layoutcolor=ffffff00
bindsym $mod+Escape exec $locker

# alignment of window title
title_align center

# border style
default_border pixel 2
default_floating_border pixel 2
hide_edge_borders smart

# workspace back and forth
workspace_auto_back_and_forth yes

# make dialogs floating
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable


### Window Behaviour

# kill focused window
bindsym $mod+Shift+q kill

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+odiaeresis focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+odiaeresis move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# resize window (you can also use the mouse for that)
mode "resize" {
	# using j, k, l, ö
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym odiaeresis resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"



### Workspaces

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# workspace to screen assignment
# LVDS-0
workspace $ws2 output LVDS-0
workspace $ws4 output LVDS-0
workspace $ws6 output LVDS-0
workspace $ws8 output LVDS-0
workspace $ws10 output LVDS-0

# HDMI-0
workspace $ws1 output HDMI-0 VGA-0 LVDI-0
workspace $ws3 output HDMI-0 VGA-0 LVDI-0
workspace $ws5 output HDMI-0 VGA-0 LVDI-0
workspace $ws7 output HDMI-0 VGA-0 LVDI-0
workspace $ws9 output HDMI-0 VGA-0 LVDI-0

# next/previous workspace
bindsym $mod+Control+Left workspace prev_on_output
bindsym $mod+Control+Right workspace next_on_output
#bindsym $mod+Control+j workspace prev
#bindsym $mod+Control+odiaeresis  workspace next

# move container to next/previous workspace
#bindsym $mod+Control+Shift+Left move container to workspace prev
#bindsym $mod+Control+Shift+Right move container to workspace next
#bindsym $mod+Control+Shift+j move container to workspace prev
#bindsym $mod+Control+Shift+odiaeresis  move container to workspace next



### Function Keys

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 10 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 10 # decrease screen brightness

# Media player controls
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
bindsym $mod+Next exec playerctl next
bindsym $mod+Prior exec playerctl previous

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
#bindsym $mod+plus exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
#bindsym $mod+minus exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
#bindsym $mod+m exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +10% && $refresh_i3status
bindsym $mod+plus exec --no-startup-id pactl set-sink-volume 0 +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -10% && $refresh_i3status
bindsym $mod+minus exec --no-startup-id pactl set-sink-volume 0 -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle && $refresh_i3status
bindsym $mod+m exec --no-startup-id pactl set-sink-mute 0 toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute 0 toggle && $refresh_i3status



### Mouse

# The middle button over a titlebar kills the window
bindsym --release button2 kill



### Application specific

# move Spotify to workspace 2
for_window [class="Spotify"] move --no-auto-back-and-forth to workspace $ws2

# open firefox stacked
for_window [class="firefox"] layout stacking

# open termite tabbed
#for_window [class=Termite] layout tabbed

# open mpv in fullscreen on workspace 9
for_window [class=mpv] move --no-auto-back-and-forth container to workspace $ws9
for_window [class=mpv] fullscreen enable



### Settings

# set keymap
exec "setxkbmap de"

# set background image
exec ~/.fehbg &

# start compositor (for transparent windows)
#exec picom -CGb -d :0 &

# i3 bars for different outputs
bar {
	output LVDS-0
	#font pango:DejaVu Sans Mono, FontAwesome 8
	#mode hide
	modifier Mod1
	bindsym button2 exec --no-startup-id i3-dmenu-desktop
        status_command i3status --config ~/.config/i3status/config_small_monitor
}

bar {
	output HDMI-0
	#font pango:DejaVu Sans Mono, FontAwesome 8
	#mode hide
	modifier Mod1
	bindsym button2 exec --no-startup-id i3-dmenu-desktop
        status_command i3status --config ~/.config/i3status/config_big_monitor
}

bar {
	output VGA-0
	#font pango:DejaVu Sans Mono, FontAwesome 8
	#mode hide
	modifier Mod1
	bindsym button2 exec --no-startup-id i3-dmenu-desktop
        status_command i3status --config ~/.config/i3status/config_small_monitor
}



### Experiments
# make window stick to the screen
bindsym $mod+Shift+s sticky toggle

# open floating/scratchpad terminal
bindsym $mod+n exec exec -a notepad termite # -e vim
for_window [instance=notepad] floating enable
for_window [instance=notepad] sticky enable
#for_window [instance=notepad] resize set 500 700
#for_window [instance=notepad] move position 832 34, move scratchpad, scratchpad show
for_window [instance=notepad] move scratchpad, scratchpad show

# move to scratchpad
bindsym $mod+Shift+x move scratchpad

# retrieve from scratchpad
bindsym $mod+x scratchpad show

# interactive i3-input
bindsym $mod+i exec i3-input -F "%s" -P 'execute i3 command: '

# toggle bar hide
bindsym $mod+Shift+d bar mode toggle

# manage displays with xrandr
set $mode_display Ext Screen (v) VGA ON, (h) HDMI ON,, (l) switch HDMI to left side (r) switch HDMI to right side, (y) VGA OFF, (x) HDMI OFF
mode "$mode_display" {
    bindsym h exec --no-startup-id xrandr --output HDMI-0 --auto --right-of LVDS-0, mode "default"
    bindsym l exec --no-startup-id xrandr --output HDMI-0 --off && xrandr --output HDMI-0 --auto --left-of LVDS-0, mode "default"
    bindsym r exec --no-startup-id xrandr --output HDMI-0 --off && xrandr --output HDMI-0 --auto --right-of LVDS-0, mode "default"
    bindsym v exec --no-startup-id xrandr --output VGA-0 --auto --right-of LVDS-0, mode "default"
    bindsym y exec --no-startup-id xrandr --output VGA-0 --off, mode "default"
    bindsym x exec --no-startup-id xrandr --output HDMI-0 --off, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+o mode "$mode_display"

# nagbar example
#exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# connect to headset
set $mode_bluetooth bluetooth: (c) connect, (d) disconnect, (f) force connect + restart pulseaudio
mode "$mode_bluetooth" {
    bindsym c exec --no-startup-id /home/max/.scripts/sony2, mode "default"
    bindsym d exec --no-startup-id /home/max/.scripts/sony_disconnect, mode "default"
    bindsym f exec --no-startup-id /home/max/.scripts/sony, mode "default"

    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+b mode "$mode_bluetooth"

#open mpv with clipboard content
bindsym $mod+y exec --no-startup-id /home/max/.scripts/mpv_clipboard

# change keyboard layout
bindsym $mod+Shift+w exec --no-startup-id setxkbmap de
